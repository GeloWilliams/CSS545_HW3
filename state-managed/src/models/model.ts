export interface ImagePage {
   imageURL: string,
   title: string
}

export interface Banner {
   order: number;
   title: string;
   url: string;
}

export interface ImageItem {
   imageURL: string;
   title: string;
}